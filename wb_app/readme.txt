wire bonder application folder, 
wb_waveform: synchronizing X, Y, Z motion and WireClamp trigger
b1w: bond one wire, tune looping-data
calculating expect voltage, w.r.t. maximum acc, jerk
force calibration
